<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/settings_nav_graph"
    app:startDestination="@id/settingsFragment">

    <action
        android:id="@+id/action_settingsNavGraph_to_authenticationNavGraph"
        app:destination="@id/authentication_nav_graph"
        app:popUpTo="@id/settings_nav_graph"
        app:popUpToInclusive="true" />

    <fragment
        android:id="@+id/settingsFragment"
        android:name="com.adamkapus.hikingapp.ui.settings.SettingsFragment"
        android:label="fragment_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>